[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[functions]
  node_bundler = "esbuild"
  included_files = ["package.json", "package-lock.json"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

